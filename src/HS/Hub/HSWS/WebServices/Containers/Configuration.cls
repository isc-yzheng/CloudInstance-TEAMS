/// Used by a web service query to return an item in a list
Class HS.Hub.HSWS.WebServices.Containers.Configuration Extends (HS.Util.WebServices.ServiceStatus, HS.Util.WebServices.ListItem) [ Inheritance = right ]
{

// Property ID As %String;

Property RegKey As %String(MAXLEN = 1000, TRUNCATE = 0);

Property RegValue As %String(MAXLEN = 1000, TRUNCATE = 0);

}
