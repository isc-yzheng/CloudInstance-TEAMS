Include (%occErrors, HS.HC)

Class HS.Util.Installer.Kit.PostInstall.Base Extends HS.Util.Installer.Kit.Base
{

/// pNamespaces can be used to determine other configuration information
ClassMethod RunRegistry(pNamespaces As HS.Util.Installer.Kit.Namespaces, pArgs...) As %Status
{
	Quit $$$ERROR($$$NotImplemented)
}

/// pNamespaces can be used to determine other configuration information
ClassMethod RunHub(pNamespaces As HS.Util.Installer.Kit.Namespaces, pArgs...) As %Status
{
	Quit $$$ERROR($$$NotImplemented)
}

/// pNamespaces can be used to determine other configuration information
ClassMethod RunEdgeGateway(pNamespaces As HS.Util.Installer.Kit.Namespaces, pArgs...) As %Status
{
	Quit $$$ERROR($$$NotImplemented)
}

/// pNamespaces can be used to determine other configuration information
ClassMethod RunAccessGateway(pNamespaces As HS.Util.Installer.Kit.Namespaces, pArgs...) As %Status
{
	Quit $$$ERROR($$$NotImplemented)
}

/// pNamespaces can be used to determine other configuration information
ClassMethod RunBus(pNamespaces As HS.Util.Installer.Kit.Namespaces, pArgs...) As %Status
{
	Quit $$$ERROR($$$NotImplemented)
}

}
