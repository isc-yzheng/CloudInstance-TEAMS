Class HS.Doc.Mapping.SDA.SourceField Extends HS.Doc.Mapping.Common.SourceField
{

Parameter XMLTYPE = "SDAPath";

Property Path As %String(MAXLEN = 1000);

Property StructuredMappingRef As %String(MAXLEN = 1000);

Property Streamlet As %String(MAXLEN = 1000);

Property XPath As %String(MAXLEN = 32000);

}
